import 'package:flutter/material.dart';

class ReportesNocturnosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('reportesnocturnos'),
      ),
      body: Center(
        child: Text('Pantalla para Reportes Nocturnos'),
      ),
    );
  }
}
