import 'package:flutter/material.dart';

class SalidaOrdenesListaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('salidaordenes'),
      ),
      body: Center(
        child: Text('Pantalla para Salida de Ordenes'),
      ),
    );
  }
}
